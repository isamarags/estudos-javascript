// Declarar uma variável chamada `myvar`, sem valor.
let myVar;

// Após declarada, atribua o valor 10 à variável `myvar`.
myVar = 10;

// Declare uma nova variável chamada `soma`, e adicione uma instrução somando os valores 15 e 8.
let soma = 15 + 8

// Atribua à variável `soma` todo o valor dela, somando 1, usando o operador de soma abreviado.
  soma++

// Atribua à variável `soma` todo o valor dela, multiplicando por 3, usando o operador de multiplicação abreviado.
  soma *= 3

// Qual é o valor da variável `soma` até aqui?
//console.log(soma); //72

// Declare uma variável chamada `souninja`, atribuindo à ela o valor booleano que representa `verdadeiro`.
let souNinja = true;

// Declare uma variável chamada `comida` que recebe um array com os valores 'arroz', 'feijão' e 'ovo'.
let comida = ['Arroz', 'Feijão', 'Ovo']

// Digite a instrução que imprime o valor de 'feijao', que está na variável `comida`.
//console.log(comida[1])

// Digite o código que verifica se a variável `soma' é igual a variável `myvar` (testando também o tipo).
/* if (soma === myVar) {
console.log('As variáveis são iguais.')
} else {
  console.log('As variáveis não são iguais.')
} */

// Digite o código que verifica se a variável `myvar` é menor ou igual à variável `soma`.
// if (myVar <= soma) {
//   console.log('A variável myVar é menor ou igual a de soma.')
// } else {
//   console.log('A variável myVar é maior que a de soma.')
// }

// Crie uma função chamada `divisao` que receba como parâmetro dois números, e retorne o resultado da divisão entre eles.


// Invoque a função criada acima, passando os parâmetros 10 e 2.




// 1) Entrar com quatro números e imprimir o cubo e a raiz cúbica de cada número.
// 2) Entrar com seis números e imprimir a raiz quadrada de cada número.
// 3) Entrar com 8 números e, para cada número, imprimir o logaritmo desse número na base 10.
// 4) Criar um algoritmo que imprima todos os números de 1 até 100 e a soma deles.
// 5) Criar um algoritmo que imprima os números pares no intervalo de 1 a 600
// 6) Imprimir todos os números de 100 até 1.
// 7) Construa um algoritmo que, para um grupo de 50 valores inteiros, determine:
//      a) A soma dos números positivos;
//      b) A quantidade de valores negativos;
// 8) Uma rainha requisitou os serviços de um monge e disse-lhe que pagaria qualquer preço. O monge, necessitando de alimentos, indagou à rainha sobre o pagamento, se poderia ser feito com grãos de trigo dispostos em um tabuleiro de xadrez (que possui 64 casas), de tal forma que o primeiro quadro deveria conter apenas um grão e os quadros subsequentes, o dobro do quadro anterior. Crie um algoritmo para calcular o total de grãos que o monge recebeu.
